apiVersion: apps/v1
kind: Deployment
metadata:
  name: awesome-jellyfish
  labels:
    app: awesome-jellyfish
spec:
  replicas: 1
  selector:
    matchLabels:
      app: awesome-jellyfish
  template:
    metadata:
      labels:
        app: awesome-jellyfish
    spec:
      containers:
        - name: awesome-jellyfish
          image: nginx:1.27
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: awesome-jellyfish
spec:
  selector:
    app: awesome-jellyfish
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
