apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: stellar-test
  annotations:
    argo.octopus.com/project: "stellar"
    argo.octopus.com/environment: "production"
    argo.octopus.com/instance: "markharrison.octopus.app"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/harrisonmeister/kubernetes-apps.git
    targetRevision: HEAD
    path: manifests/stellar-web/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: stellar-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=false
      - CreateNamespace=true