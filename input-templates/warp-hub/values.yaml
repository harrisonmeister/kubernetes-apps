replicaCount: #{Project.WarpHub.ReplicaCount}
image:
  pullPolicy: IfNotPresent
  repository: harrisonmeister/warp-hub
  tag: "#{Octopus.ProcessTemplate[Update warp-hub application manifests].Action[Update Argo CD Application Manifests].Package[Template.ContainerImage].PackageVersion}"

ingress:
  enabled: false
  annotations: {}
  hosts:
    - host: "#{Project.WarpHub.Ingress.HostName}"
      paths: []
  tls: []

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

# Default to ClusterIP for internal access only
service:
  type: ClusterIP
  port: 80
  targetPort: 8080

serviceAccount:
  create: true
  name:

podSecurityContext: {}

securityContext: {}

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
